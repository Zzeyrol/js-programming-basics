function compare_it(a,b) {

  // выводим в консоль то что мы складываем
  console.log("Результат сравнения " + a + " и " + b);

  // выводим в консоль результат сравнения

  if ( a < b ) {
    console.log('Полчается, что ' + a + ' меньше ' + b);
  } else if ( a > b ) {
    console.log('Полчается, что ' + a + ' больше ' + b);
  } else {
    console.log('Полчается, что ' + a + ' и ' + b + ' равны');
  }

  for (var i = 0; i < array.length; i++) {
    array[i]
  }

}

compare_it(2,4);

compare_it(9,7);

compare_it(1,1);
